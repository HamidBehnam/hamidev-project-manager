export enum ValidationDataSource {
    Headers,
    Body
}

export enum WorkPriority {
    Low,
    Normal,
    High,
    Urgent
}

export enum WorkStatus {
    NotStarted,
    Started,
    Done,
    Rejected,
    Accepted
}

export enum Role {
    Guest,
    Developer,
    Manager,
    Creator
}
